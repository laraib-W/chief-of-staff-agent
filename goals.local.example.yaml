# Copy to `goals.local.yaml` (gitignored) and fill in.
#
#     cp goals.local.example.yaml goals.local.yaml
#
# This file holds everything specific to you or this machine. app_sdk/run.py
# merges it over the committed `goals.yaml`, top-level key by key, so an
# `identity:` block here is the whole identity — goals.yaml has none.

identity:
  user_name: Laraib                    # how the digest greets you
  delivery_address: laraib.waheed@arbisoft.com             # where the digest is emailed
  timezone: Asia/Karachi                     # IANA name, e.g. Asia/Karachi — bounds "today"

# Written by `/plane-setup`; you do not need to fill this in by hand.
# plane:
#   projects:
#     - id: <uuid>
#       identifier: ENG
#       name: Engineering
